image: python:3.7

before_script:
    - python -V               # Print out python version for debugging
    - pip install pipenv
    - pipenv sync -d

stages:
  - test

runtests:
  stage: test
  script:
    - pipenv run nose2

runAnalysis:
  stage: test
  script:
       - pipenv run prospector
